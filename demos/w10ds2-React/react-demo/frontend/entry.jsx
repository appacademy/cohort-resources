import React from 'react';
import ReactDom from 'react-dom';

document.addEventListener("DOMContentLoaded", () => {
  // debugger;
  const root = document.getElementById('root');
  ReactDom.render(<h1>React is working bruh!</h1>, root)
});