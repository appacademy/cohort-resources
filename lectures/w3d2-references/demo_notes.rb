# Integers are IMMUTATBLE
# "=" operator changes the pointer


# Strings and Arrays are MUTABLE


# dup method creates a NEW object (array)
# << operator is used with an element
# concat must be used with an array
#  - and it will MUTATE the original array
# += will create a NEW object


# PRY tips
# reset = resets pry env
# ctrl + l = clears pry terminal