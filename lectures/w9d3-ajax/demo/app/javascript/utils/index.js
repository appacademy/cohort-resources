// Demo #1

export const fetchNewGif = () => {
  
};

export const saveGif = gif => {
  // expect gif arg to be object with keys of `title` and `url`
};

export const fetchSavedGif = title => {
  // expect title art to be title string
};