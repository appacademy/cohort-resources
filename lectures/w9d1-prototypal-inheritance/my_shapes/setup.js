// npm init -y
// creates the package.json

// npm install webpack webpack-cli
// installs these two packages locally for this project

// "start": "webpack main.js -o bundle.js --watch --mode=development"
// create a webpack script in the package.json

// npm start
// runs the newly created script

// npm uninstall -g webpack webpack-cli